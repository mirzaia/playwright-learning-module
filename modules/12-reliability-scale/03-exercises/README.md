# Exercises

Mark the capstone's critical login test as `smoke`, run it with failure artifacts, then run the broader suite with workers. Introduce a state leak, observe the parallel failure, and repair the fixture boundary. Write a short triage note from the trace rather than simply adding a retry.
