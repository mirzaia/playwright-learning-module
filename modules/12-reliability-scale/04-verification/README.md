# Verification

Run `uv run pytest -m smoke -q 03-exercises/solutions/test_reliability.py`, then run with `--tracing retain-on-failure --screenshot only-on-failure`. Explain how you would investigate a test that passes on retry but fails first.
