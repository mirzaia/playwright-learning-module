# Verification

Run `uv run pytest -q 03-exercises/solutions/test_network.py`. Review every route pattern and explain why it cannot intercept unrelated endpoints. Add a deliberately failing response and capture a trace before considering the module complete.
