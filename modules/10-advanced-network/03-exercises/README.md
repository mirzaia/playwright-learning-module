# Exercises

1. Wait for the orders API response while changing the UI filter.
2. Fulfill the API with a controlled empty response and verify the empty-state message.
3. Modify a real response by adding a synthetic order.
4. Abort image requests and verify the order workflow still works.
5. Document which test should remain an integration test rather than a mock.
