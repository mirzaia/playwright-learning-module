# Objectives

- locate and interact with controls inside an iframe;
- capture a popup or new tab without relying on timing sleeps;
- handle dialogs explicitly and safely;
- grant permissions and configure geolocation at context creation;
- control time for deterministic timer and date-picker tests;
- distinguish a browser event from a page assertion.
