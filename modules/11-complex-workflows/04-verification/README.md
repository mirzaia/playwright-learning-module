# Verification

Run `uv run pytest -q 03-exercises/solutions/test_complex.py`. Explain why each event expectation is opened before the triggering action and why a sleep would make the test less reliable.
