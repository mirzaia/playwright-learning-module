# Exercises

Implement four isolated scenarios: fill a form inside an iframe, capture a popup, accept a confirmation dialog, and verify a geolocation-aware page. Add a clock example for a countdown and explain which waits are event waits versus assertions.
