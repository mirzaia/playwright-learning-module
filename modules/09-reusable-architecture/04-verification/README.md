# Verification

Run `uv run pytest -q 03-exercises/solutions/test_architecture.py`. Confirm that the test remains readable without knowing the selectors, while the page object remains small and explicit.
