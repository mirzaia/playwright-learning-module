[project]
name = "learning-commerce-portal"
version = "0.1.0"
requires-python = ">=3.12,<3.14"
dependencies = ["flask>=3.1,<4"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/commerce_portal"]
